 {{this.id}}. {{this.name}}

    <button type = "button" class="btn btn-sm switchBtn change-watched" data-id="{{this.id}}" data-newwatched ="{{this.watched}}">
       {{#if this.watched}} Watch Again {{else}} Add to Watchlist {{/if}}
    </button>

    <button class="btn delete-movie deleteBtn" data-id="{{this.id}}"><span><i class="fas fa-minus-circle"></i></span> DELETE!</button>
