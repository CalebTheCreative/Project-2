<div class="row">
    
    {{!-- Add Meal Form --}}
    <div class="col-md-8 mt-4 justify-content-md-center">
      <h3>Add a Meal to the list:</h3>
        <form class="addMeal">
            <div class="form-group">
                <label for="addMeal">Name of Meal:</label>
                <input type="text" id="addMeal" name="name">
                <input type="hidden" name="cookedMeal" value="0">
            </div>

            <button class="btn btn-secondary mt-1" id="addMealBtn">Add your meal!</button>
        </form>
        <button class="btn btn-primary mt-1" id="decideMealBtn">Decide a meal for me!</button>
    </div>

    
</div>